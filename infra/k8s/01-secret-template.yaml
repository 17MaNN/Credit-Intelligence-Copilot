# Template only - shows what keys the Secret must contain.
# DO NOT fill in real values here and commit this file.
# Create the real Secret imperatively instead (see infra/k8s/README.md):
#   kubectl create secret generic credit-copilot-secrets \
#     --namespace=credit-copilot \
#     --from-literal=SERVICE_API_KEY=<your-value> \
#     --from-literal=GEMINI_API_KEY=<your-value>
apiVersion: v1
kind: Secret
metadata:
  name: credit-copilot-secrets
  namespace: credit-copilot
type: Opaque
stringData:
  SERVICE_API_KEY: "REPLACE_ME"
  GEMINI_API_KEY: "REPLACE_ME"